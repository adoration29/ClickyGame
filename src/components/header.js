import React from "react";

const header = () => (
  <div className="jumbotron p-3">
    <h1 className="text-center">Click the flowers!</h1>
    <h3 className="text-center">Click the  image to earn points, but don't click on any more than once!</h3>
  </div>
)
export default header;